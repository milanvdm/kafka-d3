kafka {
  bootstrap-servers = [test1, test2, test3]
  schema-registry = test
  topics = [
    {
      name = test
      partitions = 1
      replication-factor = 1
    }
  ]
}

write-side {
  url-path = system
}
